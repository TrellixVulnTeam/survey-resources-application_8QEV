/*
All material copyright ESRI, All Rights Reserved, unless otherwise specified.
See https://js.arcgis.com/4.19/esri/copyright.txt for details.
*/
import{glsl as e}from"../../shaderModules/interfaces.js";import{multipassGeometryTest as o}from"../shading/MultipassGeometryTest.glsl.js";function r(r,t){t.multipassGeometryEnabled&&r.vertex.include(o),r.vertex.code.add(e`
  void main(void) {
    vec4 posProjCenter;
    if (dot(position, position) > 0.0) {
      // Render single point to center of the pixel to avoid subpixel
      // filtering to affect the marker color
      ProjectHUDAux projectAux;
      vec4 posProj = projectPositionHUD(projectAux);
      posProjCenter = alignToPixelCenter(posProj, viewport.zw);

      ${t.multipassGeometryEnabled?e`
        // Don't draw vertices behind geometry
        if(geometryDepthTest(.5 + .5 * posProjCenter.xy / posProjCenter.w, projectAux.posView.z)){
          posProjCenter = vec4(1e038, 1e038, 1e038, 1.0);
        }`:""}

      vec3 vpos = projectAux.posModel;
      if (rejectBySlice(vpos)) {
        // Project out of clip space
        posProjCenter = vec4(1e038, 1e038, 1e038, 1.0);
      }

    }
    else {
      // Project out of clip space
      posProjCenter = vec4(1e038, 1e038, 1e038, 1.0);
    }

    gl_Position = posProjCenter;
    gl_PointSize = 1.0;
  }
  `),r.fragment.uniforms.add("color","vec4"),r.fragment.code.add(e`
  void main() {
    gl_FragColor = color;
  }
  `)}var t;!function(e){function o(e){e.setUniform4f("color",1,1,1,1)}e.bindUniforms=o}(t||(t={}));export{t as HUDOcclusion,r as HUDOcclusionPass};
